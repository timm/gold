# Installation

On Mac:

```
brew update
brew install gawk
gawk -W version | head -1
```
You want GAWK version 5+.

On Unix:

```
make gawk
```

## Testing

However you install, after installation:

```
make install
```
